# vim:tw=0:ts=2:sw=2:et:norl
# Author: Landon Bouma <https://tallybark.com/>
# Project: https://github.com/doblabs/easy-as-pypi#🥧
# License: MIT

---

name: CI tag specialist

on:
  workflow_call:
    # SAVVY: Callers call us to set values, or to fetch values, or both.
    # - So if inputs left unsaid, means caller just wants current state.

    inputs:
      # If tagged on HEAD, don't do current cascade.
      inhibit-cascade:
        type: string
        required: false

      # If tagged on any commit in current branch, don't do cascade.
      inhibit-forever:
        type: string
        required: false

      # *** Cascade-time tags
      #
      # These tags are set per-project when they start (update-deps.yml), and
      # cleared when they pass along the cascading stick (by release-smoke-test.yml,
      # after calling update-cascade.yml).

      # If tagged on HEAD and not "true", skip bin/update-poetry this cascade.
      cascade-updatedeps:
        type: string
        required: false

      ci-cascade-least:
        type: string
        required: false
      ci-cascade-alpha:
        type: string
        required: false
      ci-cascade-patch:
        type: string
        required: false
      ci-cascade-no-version-tag:
        type: string
        required: false

    # Maps workflow outputs to job outputs.
    # - SAVVY: Returns old/current values, before applying new values.
    #   - If caller is not setting new values, returns current values.
    #     - Just FYI, in case the "old-" prefix *confuses you*.
    outputs:
      old-inhibit-cascade:
        description: "The previous/maybe current ci-inhibit-cascade value"
        value: ${{ jobs.wrangle-ci-tags.outputs.old-inhibit-cascade }}
      old-inhibit-forever:
        description: "The previous/maybe current ci-inhibit-forever value"
        value: ${{ jobs.wrangle-ci-tags.outputs.old-inhibit-forever }}
      old-cascade-updatedeps:
        description: "The previous/maybe current ci-cascade-updatedeps value"
        value: ${{ jobs.wrangle-ci-tags.outputs.old-cascade-updatedeps }}
      old-cascade-least:
        description: "The old ci-cascade-least value"
        value: ${{ jobs.manage-ci-tags.outputs.old-cascade-least }}
      old-cascade-alpha:
        description: "The old ci-cascade-alpha value"
        value: ${{ jobs.manage-ci-tags.outputs.old-cascade-alpha }}
      old-cascade-patch:
        description: "The old ci-cascade-patch value"
        value: ${{ jobs.manage-ci-tags.outputs.old-cascade-patch }}
      old-cascade-skip-vers:
        description: "The old ci-cascade-no-version-tag value"
        value: ${{ jobs.manage-ci-tags.outputs.old-cascade-skip-vers }}

# ***

jobs:

  wrangle-ci-tags:
    name: Wrangle CI tags

    runs-on: ubuntu-latest

    # Maps job outputs to step outputs.
    outputs:
      old-inhibit-cascade: "${{
        steps.wrangle-ci-tags.outputs.old_inhibit_cascade
      }}"
      old-inhibit-forever: "${{
        steps.wrangle-ci-tags.outputs.old_inhibit_forever
      }}"
      old-cascade-updatedeps: "${{
        steps.wrangle-ci-tags.outputs.old_cascade_updatedeps
      }}"
      old-cascade-least: "${{
        steps.wrangle-ci-tags.outputs.old_cascade_least
      }}"
      old-cascade-alpha: "${{
        steps.wrangle-ci-tags.outputs.old_cascade_alpha
      }}"
      old-cascade-patch: "${{
        steps.wrangle-ci-tags.outputs.old_cascade_patch
      }}"
      old-cascade-skip-vers: "${{
        steps.wrangle-ci-tags.outputs.old_cascade_skip_vers
      }}"

    steps:
      - name: Checkout repo @ ${{ inputs.head-ref }}
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.repository.default_branch }}
          # We can `fetch-depth: 0` or `fetch-tags: 'true'` for all tags.
          # - We don't need history, just tags.
          # - The docs don't say why it matters. It's left to the reader
          #   to assume that fetching history and/or tags is more resource
          #   intensive and/or degrades job performance. *Just saying.*
          fetch-depth: 1
          fetch-tags: true

      # ***

      - name: Wrangle CI tags
        id: wrangle-ci-tags
        run: |
          ./.github/bin/wrangle-tags
        env:
          EAPP_INHIBIT_CASCADE: ${{ inputs.inhibit-cascade }}
          EAPP_INHIBIT_FOREVER: ${{ inputs.inhibit-forever }}
          EAPP_CASCADE_UPDATEDEPS: ${{ inputs.cascade-updatedeps }}
          EAPP_CASCADE_LEAST: ${{ inputs.ci-cascade-least }}
          EAPP_CASCADE_ALPHA: ${{ inputs.ci-cascade-alpha }}
          EAPP_CASCADE_PATCH: ${{ inputs.ci-cascade-patch }}
          EAPP_CASCADE_SKIP_VERS: ${{ inputs.ci-cascade-no-version-tag }}
