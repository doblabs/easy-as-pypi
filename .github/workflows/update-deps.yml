# vim:tw=0:ts=2:sw=2:et:norl
# Author: Landon Bouma <https://tallybark.com/>
# Project: https://github.com/doblabs/easy-as-pypi#🥧
# License: MIT

---

name: Release Cascade — Update

on:
  # Via update-cascade.yml
  repository_dispatch:
    types: [update-deps]

  # Via GitHub Actions website button
  workflow_dispatch:

run-name: 👣 — Cascade — 2.) Update deps (via ${{
  github.event_name == 'workflow_dispatch' && 'User' || 'Upstream' }})

jobs:
  poetry-update-and-update-cascade:
    uses: doblabs/easy-as-pypi/.github/workflows/update-deps-poetry.yml@release
    secrets: inherit
